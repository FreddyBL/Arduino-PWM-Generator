const settings = {
  arduinoPort: "COM5", //Will add an option to choose port number from UI later, for now set port here manually.
  serverAddress: "localhost",
  serverPort: "8080",
};

module.exports = settings;
